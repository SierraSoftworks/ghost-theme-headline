{{!< default}}

{{#post}}
    {{#if feature_image}}
        <section class="gh-cover loading gh-outer">
            <img
                class="gh-cover-image"
                srcset="{{img_url feature_image size="s" format="webp"}} 300w,
                        {{img_url feature_image size="m" format="webp"}} 720w,
                        {{img_url feature_image size="l" format="webp"}} 960w,
                        {{img_url feature_image size="xl" format="webp"}} 1200w,
                        {{img_url feature_image size="xxl" format="webp"}} 2000w"
                sizes="100vw"
                src="{{img_url feature_image size="xl" format="webp"}}"
                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                loading="eager"
                fetchpriority="high"
                decoding="async"
            >

            <div class="gh-cover-content">
                {{#if primary_tag}}
                    <a class="gh-article-tag" href="{{primary_tag.url}}">{{primary_tag.name}}</a>
                {{/if}}

                <h1 class="gh-cover-title">{{title}}</h1>
            </div>
        </section>
    {{/if}}
{{/post}}

<main class="gh-main">
    {{#post}}
        <article class="gh-article {{post_class}}">
            <header class="gh-article-header gh-canvas">
                {{> "post-meta"}}

                {{#if custom_excerpt}}
                    <p class="gh-article-excerpt">{{custom_excerpt}}</p>
                {{/if}}
            </header>

            <section class="gh-content gh-canvas">
                {{content}}
            </section>
        </article>
    {{/post}}

    {{> "related-posts"}}

    {{#post}}
        {{> "comments"}}
    {{/post}}
</main>

{{#contentFor "body_class"}} has-wide-image{{#if post.feature_image}} is-head-transparent{{/if}}{{/contentFor}}